# API Configuration
# Update this to point to your .NET 8 backend API
VITE_API_BASE_URL=http://localhost:5000

# Scheduler API Configuration (MiniProjectManager)
# This should point to the MiniProjectManager backend with scheduler endpoints
VITE_SCHEDULER_API_BASE_URL=http://localhost:5000
